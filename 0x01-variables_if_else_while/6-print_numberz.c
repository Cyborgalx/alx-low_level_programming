#include <stdio.h>

/**
 * main - ENtry point
 * Description: Print number 1 - 9
 * Return: Always 0 (Success)
 */
int main(void)
{
	char k;

	for (k = '0'; k <= '9'; k++)
	{
	putchar(k);
	}
	putchar('\n');
	return (0);
}
