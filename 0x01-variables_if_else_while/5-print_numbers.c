#include <stdio.h>

/**
 * main - ENtry point
 * Description:  a program that prints all sinle digits
 * Return: Always 0 (Success)
 */
int main(void)
{
	char d;

	for (d =  '0'; d <= '9'; d++)

	putchar(d);

	putchar('\n');
	return (0);
}
