#include "main.h"

/**
 * _abs - Computes the absolute value of integer
 * @n: The interger to be commited.
 *
 * Return: The absolute value of the integer
 */
int _abs(int n)
{
	if (n >= 0)
	return (n);
	else
	return (-n);
}
